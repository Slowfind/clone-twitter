import { makeStyles } from '@material-ui/core/styles'

export const LoaderStyles = makeStyles(() => ({
    loader: {
        display: 'block',
        textAlign: 'center',
        padding: '25px 0',
    },
}))
